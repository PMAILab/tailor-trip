import Placeholder from '../components/Placeholder';

export default function Discover() {
  return (
    <Placeholder
      title="Where is your head at today?"
      note="Mood discovery is next: pick a mood, set a budget, and choose cheapest or least crowded."
    />
  );
}
