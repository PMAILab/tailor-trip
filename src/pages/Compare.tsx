import Placeholder from '../components/Placeholder';

export default function Compare() {
  return (
    <Placeholder
      title="Compare trips"
      note="Two or three destinations side by side, best value highlighted per row, with an AI recommendation."
    />
  );
}
